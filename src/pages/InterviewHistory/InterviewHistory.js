import classes from './InterviewHistory.module.css';

const InterviewHistory = () => {
    return (
        <div className={classes.home}>
            InterviewHistory
        </div>)
}

export default InterviewHistory;